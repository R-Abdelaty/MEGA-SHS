[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "self-healing-university-scheduler"
version = "0.1.0"
description = "Provider-neutral agent scaffold for repairing university schedules."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.package-data]
self_healing_scheduler = ["prompts/*.md"]

